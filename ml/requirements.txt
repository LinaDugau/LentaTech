# ---- Backend (FastAPI + Streamlit) ----
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
pydantic==2.5.3
streamlit==1.31.0
requests==2.31.0
celery[redis]>=5.3,<6
structlog>=24.1
sentry-sdk[fastapi]>=2.0
prometheus-client>=0.20
python-jose[cryptography]>=3.3
slowapi>=0.1.9
python-magic>=0.4.27

# ---- ML core ----
pandas>=2.2.0
numpy>=1.26,<2
openpyxl>=3.1
pyarrow>=15.0
# opencv-contrib для cv2.barcode и cv2.wechat_qrcode (а не headless)
opencv-contrib-python-headless>=4.9
Pillow>=10.2
tqdm>=4.66
certifi>=2024.2

# ---- Detector + tracker ----
# torch / torchvision ставятся отдельной строкой в Dockerfile с CPU-only
# индексом (без NVIDIA CUDA пакетов). Здесь только ultralytics.
ultralytics>=8.1,<8.5

# ---- OCR ----
easyocr>=1.7
paddlepaddle>=3.0
paddleocr>=3.0

# ---- штрихкоды / QR ----
pyzbar>=0.1.9      # требует системного libzbar0
zxing-cpp>=2.2

# ---- LLM (product_name) ----
llama-cpp-python>=0.2.50

# ---- удобства ----
huggingface-hub>=0.20  # если кто-то будет скачивать GGUF
