name: lentatech-pricetag

services:
  redis:
    image: redis:7-alpine
    container_name: pricetag-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    restart: unless-stopped

  api:
    build: .
    container_name: pricetag-api
    ports:
      - "8000:8000"
    depends_on:
      - redis
    volumes:
      - ./tmp:/app/tmp
      - ./app:/app/app:ro
      - ./ml:/app/ml:ro
      - ./ml/weights:/app/ml/weights
      - ./ml/output:/app/ml/output
      - ./ml/data:/app/ml/data
      - ./db_hack.csv:/app/db_hack.csv:ro
    environment:
      - PYTHONPATH=/app
      - API_HOST=0.0.0.0
      - API_PORT=8000
      - API_PUBLIC_URL=http://localhost:8000
      - LOG_LEVEL=INFO
      - SENTRY_DSN=
      - SENTRY_ENVIRONMENT=local
      - ENABLE_WORKER_METRICS=0
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/1
      - REDIS_PROGRESS_URL=redis://redis:6379/2
      - WEBHOOK_TIMEOUT_SECONDS=5
      - JOB_FILE_RETENTION_DAYS=7
      - CLEANUP_INTERVAL_SECONDS=86400
      - REPORTING_BACKEND=parquet
      - CLICKHOUSE_DSN=
      - GREENPLUM_DSN=
      - DETECTOR_WEIGHTS_PATH=/app/ml/output/runs/pricetag_v2/weights/best.pt
      - LLM_WEIGHTS_PATH=/app/ml/weights/qwen2.5-3b-q4_k_m.gguf
      - CATALOG_PATH=/app/db_hack.csv
      - CATALOG_ENCODING=cp1251
      - CATALOG_CSV_SEP=;
      - ML_FRAME_STEP=3
      - ML_TOP_K=3
      - ML_USE_TTA=1
      # дообученные веса (если есть на диске)
      - ML_ENSEMBLE_WEIGHTS=/app/ml/output/runs/pricetag_v4/weights/best.pt,/app/ml/output/runs/pricetag_v5b/weights/best.pt
      - USE_PADDLE_OCR=1
      - USE_AGGRESSIVE_DECODE=1
      - USE_SUBMISSION_FINALIZE=1
      - MIN_FREE_DISK_BYTES=2147483648
      - AUTH_ENABLED=0
      - JWT_SECRET_KEY=change-me-in-production
      - JWT_ACCESS_TOKEN_EXPIRE_MINUTES=720
      - JWT_USERS={"admin":{"password":"admin","role":"admin"},"operator":{"password":"operator","role":"operator"},"viewer":{"password":"viewer","role":"viewer"}}
      - RATE_LIMIT_ENABLED=1
      - RATE_LIMIT_UPLOAD=5/minute
      - RATE_LIMIT_LOGIN=10/minute
      - MAX_VIDEO_DURATION_SECONDS=300
      - MIN_VIDEO_WIDTH=320
      - MIN_VIDEO_HEIGHT=240
      - MAX_VIDEO_WIDTH=3840
      - MAX_VIDEO_HEIGHT=2160
      # USE_LLM=0 → отключить Qwen (быстрее, но без product_name)
      - USE_LLM=1
      # PyTorch/EasyOCR могут плодить worker'ов — ограничим для CPU-only
      - OMP_NUM_THREADS=4
      - MKL_NUM_THREADS=4
    # ML модели нужно много RAM (EasyOCR ~1 GB, Qwen 1.8 GB)
    deploy:
      resources:
        limits:
          memory: 8G
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      # большой start_period — модели грузятся первые ~30-60 сек
      start_period: 120s
    restart: unless-stopped

  worker:
    build: .
    container_name: pricetag-worker
    depends_on:
      - redis
    ports:
      - "9101:9101"
    volumes:
      - ./tmp:/app/tmp
      - ./app:/app/app:ro
      - ./ml:/app/ml:ro
      - ./ml/weights:/app/ml/weights
      - ./ml/output:/app/ml/output
      - ./ml/data:/app/ml/data
      - ./db_hack.csv:/app/db_hack.csv:ro
    environment:
      - PYTHONPATH=/app
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/1
      - REDIS_PROGRESS_URL=redis://redis:6379/2
      - API_PUBLIC_URL=http://localhost:8000
      - LOG_LEVEL=INFO
      - SENTRY_DSN=
      - SENTRY_ENVIRONMENT=local
      - ENABLE_WORKER_METRICS=1
      - PROMETHEUS_WORKER_PORT=9101
      - WEBHOOK_TIMEOUT_SECONDS=5
      - JOB_FILE_RETENTION_DAYS=7
      - CLEANUP_INTERVAL_SECONDS=86400
      - DETECTOR_WEIGHTS_PATH=/app/ml/output/runs/pricetag_v2/weights/best.pt
      - LLM_WEIGHTS_PATH=/app/ml/weights/qwen2.5-3b-q4_k_m.gguf
      - CATALOG_PATH=/app/db_hack.csv
      - CATALOG_ENCODING=cp1251
      - CATALOG_CSV_SEP=;
      - ML_FRAME_STEP=3
      - ML_TOP_K=3
      - ML_USE_TTA=1
      # дообученные веса (если есть на диске)
      - ML_ENSEMBLE_WEIGHTS=/app/ml/output/runs/pricetag_v4/weights/best.pt,/app/ml/output/runs/pricetag_v5b/weights/best.pt
      - USE_PADDLE_OCR=1
      - USE_AGGRESSIVE_DECODE=1
      - USE_SUBMISSION_FINALIZE=1
      - USE_LLM=1
      # Paddle вызывается в subprocess с OMP=1; здесь можно держать потоки для YOLO/EasyOCR
      - OMP_NUM_THREADS=4
      - MKL_NUM_THREADS=4
    deploy:
      resources:
        limits:
          # v2+v4+v5b+TTA + 720 tracks: нужно 20G+ (Docker Desktop → Settings → Memory)
          memory: 24G
    command: celery -A app.queue worker --concurrency=1 --loglevel=info
    healthcheck:
      disable: true
    restart: unless-stopped

  beat:
    build: .
    container_name: pricetag-beat
    depends_on:
      - redis
    volumes:
      # beat читает SQLite и ставит периодическую задачу очистки в Redis
      - ./tmp:/app/tmp
    environment:
      - PYTHONPATH=/app
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/1
      - LOG_LEVEL=INFO
      - SENTRY_DSN=
      - SENTRY_ENVIRONMENT=local
      - ENABLE_WORKER_METRICS=0
      - PROMETHEUS_WORKER_PORT=9101
      - JOB_FILE_RETENTION_DAYS=7
      - CLEANUP_INTERVAL_SECONDS=86400
    command: celery -A app.queue beat --loglevel=info
    healthcheck:
      disable: true
    restart: unless-stopped

  ui:
    build: .
    container_name: pricetag-ui
    ports:
      - "8501:8501"
    depends_on:
      api:
        condition: service_healthy
    volumes:
      - ./app:/app/app:ro
    environment:
      - PYTHONPATH=/app
      - API_URL=http://api:8000
      - API_TOKEN=
    command: streamlit run app/ui.py --server.port=8501 --server.address=0.0.0.0
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8501/_stcore/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    restart: unless-stopped

volumes:
  tmp_data:
    driver: local
  redis_data:
    driver: local
